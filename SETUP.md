# Setup & Launch

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python3 main.py
```

First time only — after that just:

```bash
source .venv/bin/activate && python3 main.py
```
